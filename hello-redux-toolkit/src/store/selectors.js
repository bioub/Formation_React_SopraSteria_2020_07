// import { createSelector } from '@reduxjs/toolkit';

// createSelector est le createSelector de reselector

export function selectCount(state) {
  return state.count;
}

export function selectStep(state) {
  return state.step;
}